import React from 'react';
import Layout from '@theme/Layout';

function DeploymentGuides() {
  return (
    <Layout title="DeploymentGuides">
      <div
        style={{
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          height: '50vh',
          fontSize: '20px',
        }}>
        <p>
          This is a custom <code> DeploymentGuides page</code> .
        </p>
      </div>
    </Layout>
  );
}

export default DeploymentGuides;